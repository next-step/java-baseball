package org.dhlee.game.utils;

public class CompareUtils {

    public static boolean compareNumbers(int value, int compare) {
        if (value == compare) {
            return true;
        }
        return false;
    }
}
